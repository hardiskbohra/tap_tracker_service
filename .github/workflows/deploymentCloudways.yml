name: 🚀 Deploy to Cloudways
on:
  push:
    branches:
      - master
      
jobs:
  deploy-job:
    name: SFTP-Deploy-Action
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 2
    - name: 📂 Sync files
      uses: wlixcc/SFTP-Deploy-Action@v1.2.4
      with:
        server: phpstack-647823-2922742.cloudwaysapps.com
        username: tap_admin123
        password: Tap_admin123
        local_path: './*'
        remote_path: 'sudo chown -R $tap_admin123:$tap_admin123 /'
      
    
